{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hr.dev/change/v1",
  "title": "CHANGE",
  "description": "Engineering. What was built, at which reversibility rung, and what it put back into the knowledge base.",
  "type": "object",
  "required": ["serves", "rung", "routed"],
  "additionalProperties": false,
  "properties": {
    "serves": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "description": "Exactly one of the two legal queues. Neither means the work is unattributed, which is allowed but must be said out loud.",
      "properties": {
        "specId": { "type": "string" },
        "findingId": { "type": "string" }
      }
    },
    "rung": { "enum": ["revert", "flag", "expand", "migrate", "emit", "destroy"] },
    "constraintsTouched": { "type": "array", "items": { "type": "string" } },
    "routed": {
      "type": "object",
      "required": ["c", "o", "r"],
      "additionalProperties": false,
      "description": "What this change wrote back to each knowledge layer. Empty is legitimate on a small change and suspicious on a large one.",
      "properties": {
        "c": { "type": "array", "items": { "type": "string" } },
        "o": { "type": "array", "items": { "type": "string" } },
        "r": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
